import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class PayboxIntegrationService {
  initPaybox() {
    console.log('Inicializando botones o logica especifica de Paybox...');
  }
}
